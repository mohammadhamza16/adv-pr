# MySQL configuration
db.url=jdbc:mysql://localhost:3306/ecommerce?useSSL=false&serverTimezone=UTC
db.user=root
db.password=changeme

generated.jwt.secret=change_this_secret_key_to_a_stronger_one
jwt.expiration-ms=86400000

# Redis configuration
redis.host=localhost
redis.port=6379
redis.timeout-ms=2000

# Cache configuration
product.cache.key=products:list
product.cache.ttl.seconds=120
payment.lock.ttl.seconds=30
